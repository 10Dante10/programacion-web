function Booleanas(objeto){
   const Booleano ={
     a : True,
     b: "hello",
     c : false,
   }
   const properties = Object.keys(spiderman)
console.log(properties.length) // 3
properties.forEach(property => {
  console.log(property)
 }
}
